---
phase: 05-experimental-modifier-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/calculator/interactive-calculator.tsx
  - components/calculator/calculator-result-panel.tsx
  - components/calculator/experimental-modifiers-panel.tsx
  - lib/calc/modifiers.ts
  - tests/unit/calc/calculate-fresh-yeast.test.ts
  - tests/integration/home-page.test.tsx
  - tests/e2e/home.spec.ts
autonomous: true
requirements:
  - EXP-01
  - EXP-02
user_setup: []
must_haves:
  truths:
    - The calculator keeps one active empirical model, and experimental modifiers remain visibly separate from that active math.
    - The advanced section is present but closed by default, clearly secondary, and does not compete with the primary calculator workflow.
    - All seven future modifier categories are visible through typed UI affordances that match the existing domain seam.
    - Entering experimental modifier values never changes the active grams output in v1.
    - Reset restores both the main calculator state and the advanced placeholder state, including the accordion visibility.
    - The result and explanatory copy make it explicit that experimental modifier values are present but not applied in the MVP.
  artifacts:
    - path: components/calculator/interactive-calculator.tsx
      provides: calculator-local orchestration for advanced-section state, modifier draft values, and reset behavior
      contains: InteractiveCalculator
    - path: components/calculator/experimental-modifiers-panel.tsx
      provides: closed-by-default advanced accordion and visible inactive modifier controls
      contains: ExperimentalModifiersPanel
    - path: lib/calc/modifiers.ts
      provides: aligned modifier metadata and no-op modifier summary behavior for UI and domain reuse
      contains: summarizeAppliedModifiers
    - path: components/calculator/calculator-result-panel.tsx
      provides: trustworthy rendering of experimental placeholder status alongside active result context
      contains: CalculatorResultPanel
    - path: tests/integration/home-page.test.tsx
      provides: regression coverage for accordion behavior, inactive control interaction, and reset semantics
      contains: home page calculator flow
    - path: tests/e2e/home.spec.ts
      provides: browser-level proof that the advanced section remains accessible and non-disruptive
      contains: calculator flow is reachable, interactive, and accessible
    - path: tests/unit/calc/calculate-fresh-yeast.test.ts
      provides: durable proof that populated experimental modifiers do not alter v1 math
      contains: calculateFreshYeast
  key_links:
    - from: components/calculator/interactive-calculator.tsx
      to: components/calculator/experimental-modifiers-panel.tsx
      via: the calculator boundary owns accordion state and passes typed placeholder modifier state into a dedicated advanced component
      pattern: modifiers|advanced|accordion
    - from: components/calculator/interactive-calculator.tsx
      to: lib/calc/calculate-fresh-yeast.ts
      via: modifier placeholder state flows through the existing domain contract while numeric output remains unchanged
      pattern: calculateFreshYeast|modifiers
    - from: lib/calc/modifiers.ts
      to: components/calculator/experimental-modifiers-panel.tsx
      via: one aligned registry prevents UI/domain drift for the seven future categories
      pattern: hydration|salt|sugar|fat|flourStrength|fermentationStage|coldRetard
    - from: components/calculator/interactive-calculator.tsx
      to: components/calculator/calculator-result-panel.tsx
      via: result-state derivation keeps the user-facing experimental status honest even when placeholder values are present
      pattern: modifierStatus|summary|inactive
---

<objective>
Plan and execute the full Phase 5 experimental seam pass in one coherent wave.

Purpose: Add a closed-by-default advanced section for future modifiers, let users inspect and fill placeholder controls safely, preserve the current v1 math completely unchanged, and lock that inactive-seam behavior with durable regression coverage.
Output: An advanced accordion, typed future-factor affordances aligned with the existing modifier seam, and tests that prove modifier presence never contaminates the active estimator.
</objective>

<execution_context>
@C:/Users/peppeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/peppeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-experimental-modifier-seam/05-CONTEXT.md
@.planning/phases/05-experimental-modifier-seam/05-RESEARCH.md
@.planning/phases/05-experimental-modifier-seam/05-VALIDATION.md
@.planning/phases/04-trust-centered-premium-presentation/04-01-PLAN.md
@.planning/phases/02-domain-calculation-engine/02-01-PLAN.md
@base.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the future-factor registry and advanced accordion surface</name>
  <files>lib/calc/modifiers.ts, components/calculator/experimental-modifiers-panel.tsx, components/calculator/interactive-calculator.tsx</files>
  <action>Extend the existing experimental modifier seam into one typed source of truth that the UI can render without duplicating category knowledge. Keep the seven existing factor keys aligned with the current domain contract and add the minimum metadata required to render understandable placeholder affordances, such as labels, control kinds, helper copy, and any select options. Use that metadata to introduce a dedicated advanced accordion component that is closed by default, visually secondary, and explicit that the controls are experimental placeholders not applied to the current v1 estimate.</action>
  <verify>
    <automated>npm run test:integration</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The product exposes a sober advanced section with all seven future modifier categories, and the UI/source-of-truth mapping is typed rather than duplicated ad hoc.</done>
</task>

<task type="auto">
  <name>Task 2: Wire modifier placeholder state, result honesty, and reset semantics without touching the math</name>
  <files>components/calculator/interactive-calculator.tsx, components/calculator/calculator-result-panel.tsx, lib/calc/modifiers.ts</files>
  <action>Introduce calculator-local state for accordion visibility and experimental modifier placeholder values, then pass that state through the existing `calculateFreshYeast` contract without changing the numerical output path. Preserve the current live result behavior, warning semantics, and main guidance/invalid/success flow. Ensure reset restores the accordion to closed and clears all placeholder values. If placeholder values are present, the result surface should still make it obvious that modifiers are present but not applied in the MVP, without presenting them like active correction coefficients.</action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npm run test:integration</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Users can open the advanced section and explore future inputs, but the active estimate remains the same and the product stays honest about that fact.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the inactive experimental seam with unit, integration, and browser regression coverage</name>
  <files>tests/unit/calc/calculate-fresh-yeast.test.ts, tests/integration/home-page.test.tsx, tests/e2e/home.spec.ts</files>
  <action>Extend tests so Phase 5 protects behavior rather than just presence. Unit coverage must continue proving that populated modifier state does not alter grams output. Integration coverage must verify the accordion is closed by default, all seven categories become visible when opened, placeholder interaction does not change the active result, and reset clears advanced state as well as core calculator state. The browser smoke should confirm the advanced section remains reachable and accessible without disrupting the main calculator flow.</action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npm run test:integration</automated>
    <automated>npm run test:e2e</automated>
  </verify>
  <done>The repository has durable proof that Phase 5 adds future-facing affordances without contaminating the v1 model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test:unit succeeds
- [ ] npm run test:integration succeeds
- [ ] npm run typecheck succeeds
- [ ] npm run test:e2e succeeds
- [ ] The advanced section is closed by default and visually secondary to the main calculator workflow
- [ ] All seven future modifier categories are present and aligned with the current domain seam
- [ ] Editing experimental values does not change the active grams result in the MVP
- [ ] Reset clears advanced placeholder values and closes the advanced section
- [ ] The result surface still communicates clearly which model is active and that experimental modifiers remain inactive in v1
</verification>

<success_criteria>

- All tasks completed
- The UI includes a closed-by-default advanced section dedicated to experimental modifiers
- The seven future factor categories are represented through typed data structures and visible UI affordances
- Experimental controls can be explored without changing the active calculator math
- Reset behavior restores both core and advanced calculator state
- The product remains honest and focused: one active model, one calculator, visible future scope, no fake precision
- Automated regression coverage proves that modifier presence does not alter the base-model result in v1
- Phase 5 satisfies EXP-01 and EXP-02 without opening scope into validated advanced modeling
</success_criteria>

<output>
After completion, create `.planning/phases/05-experimental-modifier-seam/05-01-SUMMARY.md`
</output>