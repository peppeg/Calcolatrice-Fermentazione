---
phase: 02-domain-calculation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/calc/constants.ts
  - lib/calc/types.ts
  - lib/calc/messages.ts
  - lib/calc/normalize.ts
  - lib/calc/model.ts
  - lib/calc/rounding.ts
  - lib/calc/validation.ts
  - lib/calc/modifiers.ts
  - lib/calc/calculate-fresh-yeast.ts
  - lib/calc/index.ts
  - tests/unit/calc/normalize.test.ts
  - tests/unit/calc/model.test.ts
  - tests/unit/calc/validation.test.ts
  - tests/unit/calc/rounding.test.ts
  - tests/unit/calc/calculate-fresh-yeast.test.ts
autonomous: true
requirements:
  - SAFE-02
  - SAFE-03
user_setup: []
must_haves:
  truths:
    - The codebase contains a pure, reusable domain engine for the empirical fresh-yeast model with clear module boundaries.
    - The calculation flow normalizes flour units, validates inputs, preserves non-blocking empirical warnings, and applies practical rounding only at the terminal output step.
    - Experimental modifiers remain an explicit seam in the result contract without altering MVP math.
    - Domain-focused unit tests lock the formula, scaling, conversion, validation, warning, and rounding behavior with reliable numeric assertions.
  artifacts:
    - path: lib/calc/calculate-fresh-yeast.ts
      provides: primary domain orchestrator for future UI phases
      contains: calculateFreshYeast
    - path: lib/calc/model.ts
      provides: raw empirical formula and flour scaling helpers
      contains: calculateBaseYeastPerKg
    - path: lib/calc/validation.ts
      provides: field-specific validation and empirical warning derivation
      contains: outside-empirical-range
    - path: lib/calc/rounding.ts
      provides: practical rounding utility used by final outputs
      contains: roundPracticalYeast
    - path: tests/unit/calc/calculate-fresh-yeast.test.ts
      provides: end-to-end domain contract verification
      contains: calculateFreshYeast
  key_links:
    - from: lib/calc/calculate-fresh-yeast.ts
      to: lib/calc/normalize.ts
      via: canonical flour normalization before validation and math
      pattern: normalize
    - from: lib/calc/calculate-fresh-yeast.ts
      to: lib/calc/validation.ts
      via: errors dominate warnings and invalid results short-circuit
      pattern: validate|warning
    - from: lib/calc/calculate-fresh-yeast.ts
      to: lib/calc/rounding.ts
      via: terminal practical rounding for both per-kg and recipe outputs
      pattern: roundPracticalYeast
---

<objective>
Establish the complete Phase 2 domain engine in one coherent pass.

Purpose: Implement the empirical calculation pipeline, canonical input normalization, field-specific validation, sober warning behavior, practical rounding, and the inactive modifier seam without leaking UI work from later phases.
Output: A pure `lib/calc` domain package and a domain-heavy unit test suite that Phase 3 can consume directly.
</objective>

<execution_context>
@C:/Users/peppeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/peppeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-domain-calculation-engine/02-CONTEXT.md
@.planning/phases/02-domain-calculation-engine/02-RESEARCH.md
@.planning/phases/02-domain-calculation-engine/02-VALIDATION.md
@base.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the calc-domain contract and canonical input layer</name>
  <files>lib/calc/constants.ts, lib/calc/types.ts, lib/calc/messages.ts, lib/calc/normalize.ts, lib/calc/modifiers.ts, lib/calc/index.ts</files>
  <action>Create the reusable `lib/calc` domain package and define the durable contract that later phases will consume. Add strict domain types for UI-shaped input, normalized input, structured validation issues, structured warnings, result objects, and the experimental modifier seam. Centralize immutable formula constants, empirical intervals, and any message-map primitives needed to keep the domain copy stable. Implement the flour normalization layer that converts `g|kg` input into canonical `flourGrams` without mixing in validation or warning logic. Keep the modifier seam explicit but inactive: it should summarize active state without introducing any numeric correction.</action>
  <verify>npm run typecheck; npm run test:unit</verify>
  <done>The codebase has a clean `lib/calc` contract with canonical normalized input and a no-op modifier seam ready for the pure calculation engine.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the pure empirical calculation pipeline</name>
  <files>lib/calc/model.ts, lib/calc/rounding.ts, lib/calc/validation.ts, lib/calc/calculate-fresh-yeast.ts, lib/calc/index.ts</files>
  <action>Implement the raw empirical helpers for grams-per-kilogram and flour scaling using the approved formula and unrounded floating-point math. Add field-specific validation for missing, non-finite, zero, and negative values. Add the single sober empirical-range warning model that triggers when either temperature or time leaves the approved interval and reports a compact reason. Implement the fixed practical rounding rule as the final output-only step: values greater than or equal to 1 gram round to 2 decimals, and values below 1 gram round to 3 decimals. Use the same rule for both per-kilogram and recipe outputs. Compose everything in `calculateFreshYeast` with the researched sequence: normalize, validate, short-circuit invalid results, calculate raw outputs, derive warnings, apply rounding, and return a stable result contract with the inactive modifier summary.</action>
  <verify>npm run test:unit; npm run typecheck</verify>
  <done>The domain engine computes trustworthy outputs, keeps warnings non-blocking, suppresses warnings on invalid input, and exposes a stable public API for later UI phases.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the domain behavior with strong numeric tests</name>
  <files>tests/unit/calc/normalize.test.ts, tests/unit/calc/model.test.ts, tests/unit/calc/validation.test.ts, tests/unit/calc/rounding.test.ts, tests/unit/calc/calculate-fresh-yeast.test.ts</files>
  <action>Add domain-heavy unit tests that prove normalization, formula math, flour scaling, validation errors, empirical warnings, the fixed rounding thresholds (`>= 1 -> 2 decimals`, `< 1 -> 3 decimals`), and orchestrator behavior. Use exact assertions where arithmetic is exact and `toBeCloseTo` for exponent-based formula checks. Cover inclusive boundaries for empirical ranges, just-outside warning cases, invalid input suppression, modifier no-op behavior, and at least one small monotonicity sweep so accidental regressions in the core math are caught early. Keep integration and e2e scope unchanged; Phase 2 should close with domain confidence rather than UI expansion.</action>
  <verify>npm run test:unit; npm run validate:quick</verify>
  <done>The phase has a serious numeric safety net and the repository-wide quick validation remains green after the new domain engine lands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck succeeds
- [ ] npm run test:unit succeeds
- [ ] npm run validate:quick succeeds
- [ ] The `lib/calc` package stays pure and framework-free
- [ ] Phase 2 covers SAFE-02 and SAFE-03 directly while establishing the pure domain contract required by later interactive phases
</verification>

<success_criteria>

- All tasks completed
- The repository contains a reusable pure calculation package under `lib/calc`
- Canonical flour normalization, empirical warnings, and the fixed practical rounding rule (`>= 1 -> 2 decimals`, `< 1 -> 3 decimals`) are implemented as domain logic
- The public calculation contract is stable enough for Phase 3 live UI integration
- Unit tests lock the math and domain behavior with trustworthy numeric assertions
- Planning artifacts remain intact and the phase stays inside the domain-engine boundary
- Phase 2 satisfies SAFE-02 and SAFE-03 while establishing the pure calculation contract that Phase 3 will consume for interactive input handling
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-calculation-engine/02-01-SUMMARY.md`
</output>

