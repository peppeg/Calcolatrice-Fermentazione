---
phase: 01-foundation-and-quality-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - eslint.config.mjs
  - prettier.config.mjs
  - vitest.config.ts
  - playwright.config.ts
  - .gitignore
  - .gitattributes
  - .github/workflows/ci.yml
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - tests/setup.ts
  - tests/unit/app-shell.test.ts
  - tests/integration/home-page.test.tsx
  - tests/e2e/home.spec.ts
autonomous: true
requirements:
  - ENG-01
  - ENG-02
user_setup: []
must_haves:
  truths:
    - The repository root contains a runnable Next.js App Router baseline without disturbing existing planning artifacts.
    - The project exposes real npm commands for lint, typecheck, unit test, integration test, e2e test, build, and quick/full validation.
    - GitHub Actions enforces the same quality contract that developers use locally.
    - Repository hygiene is in place for generated artifacts and cross-platform line endings.
  artifacts:
    - path: package.json
      provides: root package baseline and npm quality contract
      contains: '"validate:full"'
    - path: app/page.tsx
      provides: placeholder application shell for baseline verification
      exports:
        - default
    - path: vitest.config.ts
      provides: unit and integration test runner configuration
      contains: environment
    - path: playwright.config.ts
      provides: browser smoke test configuration
    - path: .github/workflows/ci.yml
      provides: repeatable CI quality enforcement
      contains: quality
    - path: .gitattributes
      provides: Windows/Linux line-ending normalization
  key_links:
    - from: package.json
      to: vitest.config.ts
      via: unit and integration test scripts
      pattern: 'test:(unit|integration)'
    - from: package.json
      to: playwright.config.ts
      via: e2e and validation scripts
      pattern: 'test:e2e|validate:full'
    - from: .github/workflows/ci.yml
      to: package.json
      via: npm script execution in CI
      pattern: 'npm run'
---

<objective>
Establish the complete Phase 1 engineering baseline in one coherent pass.

Purpose: Create a safe bootstrap, strict npm script contract, baseline test harness, and GitHub-ready CI without leaking product-domain work into later phases.
Output: Root Next.js app shell, repo hygiene files, quality tooling configs, smoke tests, and CI workflow.
</objective>

<execution_context>
@C:/Users/peppeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/peppeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-quality-baseline/01-CONTEXT.md
@.planning/phases/01-foundation-and-quality-baseline/01-RESEARCH.md
@.planning/phases/01-foundation-and-quality-baseline/01-VALIDATION.md
@base.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the root app safely and cleanly</name>
  <files>package.json, package-lock.json, tsconfig.json, next.config.ts, postcss.config.mjs, eslint.config.mjs, prettier.config.mjs, .gitignore, .gitattributes, app/layout.tsx, app/page.tsx, app/globals.css</files>
  <action>Use the researched bootstrap strategy for a non-empty repo: scaffold a minimal Next.js 16 App Router app in a temporary sibling directory with `create-next-app --empty --disable-git --skip-install`, then transplant only the needed app and config files into the real repository root. Preserve `.planning/`, `.git/`, and `base.md` untouched. After transplant, align the root config to the approved stack: npm metadata, strict TypeScript, Tailwind-ready globals, explicit ESLint CLI config compatible with Next.js 16, Prettier baseline, clean `.gitignore`, and `.gitattributes` that normalizes LF for Linux CI. Keep `app/page.tsx` intentionally minimal as a bootstrap harness rather than calculator product work.</action>
  <verify>npm install; npm run build; npm run lint; npm run typecheck</verify>
  <done>The root app exists, builds cleanly, lints, typechecks, and repository hygiene files are in place without disturbing planning artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the strict test and validation contract</name>
  <files>package.json, vitest.config.ts, playwright.config.ts, tests/setup.ts, tests/unit/app-shell.test.ts, tests/integration/home-page.test.tsx, tests/e2e/home.spec.ts</files>
  <action>Configure Vitest with a workable unit and integration baseline, add the shared DOM test setup, and create smoke-level tests for the placeholder application shell. Configure Playwright with a stable local server strategy suitable for CI and add a home-page browser smoke test; include lightweight accessibility smoke if it can be added cleanly within the baseline. Expose the exact npm script contract required by ENG-01 and the validation strategy: `test`, `test:unit`, `test:integration`, `test:e2e`, `validate:quick`, and `validate:full`. Keep all commands non-watch and CI-safe.</action>
  <verify>npm run test:unit; npm run test:integration; npm run test:e2e; npm run validate:quick</verify>
  <done>Unit, integration, and browser smoke tests run through npm scripts and the quick/full validation commands reflect the agreed strict baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Add GitHub Actions enforcement and close the phase baseline</name>
  <files>.github/workflows/ci.yml, package.json, .gitattributes</files>
  <action>Create a GitHub Actions workflow with a fast `quality` job and a dependent `e2e` job that mirrors the local npm contract. Use `npm ci`, explicit npm script steps, and useful failure artifacts without adding deployment or release logic. Ensure the workflow and final file set satisfy both repository seriousness and GitHub readiness. Finish by running the full validation contract so the phase ends with real evidence, not inferred correctness.</action>
  <verify>npm run validate:full; npm run build</verify>
  <done>CI enforces the same strict contract used locally, the baseline is GitHub-ready, and the full validation path is green.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint succeeds
- [ ] npm run typecheck succeeds
- [ ] npm run test:unit succeeds
- [ ] npm run test:integration succeeds
- [ ] npm run test:e2e succeeds
- [ ] npm run validate:full succeeds
- [ ] The app shell still builds and opens as a minimal baseline app
</verification>

<success_criteria>

- All tasks completed
- The repository root now contains a working Next.js App Router baseline
- The full npm quality contract exists and is executable
- Minimal smoke coverage exists for unit, integration, and browser paths
- GitHub Actions enforces the same core contract as local validation
- Planning artifacts remain intact and untouched in their existing locations
- Phase 1 satisfies ENG-01 and ENG-02 without leaking calculator-domain implementation into later phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-quality-baseline/01-01-SUMMARY.md`
</output>
