---
phase: 03-interactive-calculator-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - components/calculator/interactive-calculator.tsx
  - components/calculator/calculator-result-panel.tsx
  - lib/calc/calculator-draft.ts
  - tests/unit/calc/calculator-draft.test.ts
  - tests/integration/home-page.test.tsx
  - tests/e2e/home.spec.ts
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - CALC-03
  - CALC-04
  - CALC-05
  - SAFE-01
user_setup: []
must_haves:
  truths:
    - The homepage remains a server-rendered shell that mounts a dedicated client calculator boundary instead of converting the entire page to client state.
    - Editable calculator fields are modeled as string draft state so blank, partial, malformed, zero, and negative input can be distinguished cleanly.
    - The result region stays mounted and derives a single guidance, invalid, success, or success-with-warning variant from the current draft without preserving stale numbers.
    - Validation feedback appears both near the affected field and as a compact summary in the result area, and both surfaces explicitly name the affected fields.
    - UI logic handles only presence and parseability while `calculateFreshYeast` remains the sole source of numeric validation, normalization, warnings, and final result truth.
    - Presets update only temperature and time, and reset restores the exact initial calculator state including result-mode and preset-selection state.
    - User-facing tests replace the Phase 1 placeholder checks with calculator interaction coverage for live updates, suspended results, presets, reset, and browser accessibility smoke.
  artifacts:
    - path: components/calculator/interactive-calculator.tsx
      provides: client-side calculator orchestration and input state ownership
      contains: InteractiveCalculator
    - path: lib/calc/calculator-draft.ts
      provides: UI-only draft parsing and result-mode derivation
      contains: parseCalculatorDraft
    - path: components/calculator/calculator-result-panel.tsx
      provides: stable mounted result region for guidance, invalid, and success states
      contains: CalculatorResultPanel
    - path: tests/integration/home-page.test.tsx
      provides: component-level live calculator regression coverage
      contains: home page calculator flow
    - path: tests/e2e/home.spec.ts
      provides: browser-level calculator smoke and accessibility verification
      contains: calculator experience
  key_links:
    - from: app/page.tsx
      to: components/calculator/interactive-calculator.tsx
      via: server page shell mounts the isolated interactive calculator boundary
      pattern: InteractiveCalculator
    - from: components/calculator/interactive-calculator.tsx
      to: lib/calc/calculator-draft.ts
      via: current draft strings are parsed into guidance, invalid, or domain-ready input on every render
      pattern: parse|draft|missingFields
    - from: components/calculator/interactive-calculator.tsx
      to: lib/calc/calculate-fresh-yeast.ts
      via: domain engine remains the single source of validation, normalization, warning, and numeric output truth
      pattern: calculateFreshYeast
    - from: components/calculator/interactive-calculator.tsx
      to: components/calculator/calculator-result-panel.tsx
      via: a derived view model drives one stable mounted result region and the result-summary error surface
      pattern: result|variant|warning|summary
---

<objective>
Establish the complete Phase 3 live calculator flow in one coherent pass.

Purpose: Replace the placeholder homepage with a real interactive calculator that accepts live input, suspends results cleanly for incomplete or invalid states, reuses the existing domain engine for all numeric truth, and adds regression coverage for presets, reset, and accessibility.
Output: A dedicated client calculator experience mounted from `app/page.tsx`, a UI-only draft-validation seam, and user-facing tests that prove the interaction contract.
</objective>

<execution_context>
@C:/Users/peppeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/peppeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-interactive-calculator-flow/03-CONTEXT.md
@.planning/phases/03-interactive-calculator-flow/03-RESEARCH.md
@.planning/phases/03-interactive-calculator-flow/03-VALIDATION.md
@.planning/phases/02-domain-calculation-engine/02-01-PLAN.md
@base.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the calculator draft-state and parsing seam</name>
  <files>lib/calc/calculator-draft.ts, tests/unit/calc/calculator-draft.test.ts</files>
  <action>Create a UI-only helper layer for Phase 3 that models editable calculator values as strings and exposes the initial draft state, missing-field detection, parse-issue detection, and conversion into `CalculatorInput` only when all required fields are present and parseable. Keep this layer intentionally narrow: it should distinguish blank input from malformed numbers for `SAFE-01`, but it must not duplicate domain positivity rules or empirical warning thresholds. Add focused unit tests that lock the blank vs malformed distinction, required-field guidance, and exact reset-to-initial-state behavior for the draft model.</action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The UI has a pure draft-state seam that can drive guidance and invalid result modes without collapsing empty fields into generic numeric failures.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the placeholder shell with the live calculator, stable result panel, presets, and reset</name>
  <files>app/page.tsx, components/calculator/interactive-calculator.tsx, components/calculator/calculator-result-panel.tsx</files>
  <action>Replace the current placeholder homepage with a server-rendered shell that mounts a dedicated client `InteractiveCalculator` component. Implement clear labeled controls for temperature, time, flour quantity, and flour unit; derive calculator output on every render without a submit button; and feed a single result-panel view model into a mounted `CalculatorResultPanel`. The result panel must support the mutually exclusive states defined in phase context: guidance for missing input, invalid for parseable-but-not-calculable input, success for valid results, and success-with-warning when the domain engine reports empirical-range warnings. Success states must show both `gramsForRecipe` and `gramsPerKg`, and invalid or incomplete edits must remove stale numeric output immediately. Add preset controls that update only `temperatureC` and `timeHours`, plus one reset path backed by the shared initial-state constant. Validation feedback is a locked product decision in this phase: render adjacent field errors and a compact result-area summary at the same time, and ensure both surfaces explicitly name the affected fields.</action>
  <verify>
    <automated>npm run test:integration</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The homepage exposes the full live calculator flow for CALC-01, CALC-02, CALC-03, CALC-04, CALC-05, and SAFE-01 with a stable mounted result region and dual-surface validation feedback.</done>
</task>

<task type="auto">
  <name>Task 3: Replace placeholder test coverage with calculator interaction and accessibility checks</name>
  <files>tests/integration/home-page.test.tsx, tests/e2e/home.spec.ts, tests/unit/calc/calculator-draft.test.ts</files>
  <action>Rewrite the existing placeholder integration and e2e tests to reflect the real calculator flow. Integration coverage must prove the initial guidance state, live valid updates without submit, both displayed output values, clean suspension for empty or malformed drafts, field-specific invalid feedback for zero or negative values, the compact result-area summary for invalid states, valid-with-warning rendering for out-of-range inputs, preset partial updates, and full reset behavior. Keep the Playwright smoke lightweight but real: load the page, enter a valid scenario, confirm both outputs are visible, exercise either preset or reset, and run the axe scan on the rendered calculator experience.</action>
  <verify>
    <automated>npm run test:integration</automated>
    <automated>npx playwright test tests/e2e/home.spec.ts</automated>
  </verify>
  <done>The repository has durable regression coverage for the interactive calculator instead of the original Phase 1 placeholder shell, including proof of both inline and result-summary validation feedback.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck succeeds
- [ ] npm run test:unit succeeds
- [ ] npm run test:integration succeeds
- [ ] npm run validate:quick succeeds
- [ ] npm run test:e2e succeeds
- [ ] `app/page.tsx` remains a server component that mounts, rather than replaces itself with, the client calculator boundary
- [ ] The result region stays mounted across guidance, invalid, success, and warning states without leaving stale numeric output visible
- [ ] Invalid states show both adjacent field errors and a compact result-area summary that explicitly names the affected fields
- [ ] UI parsing logic stays limited to presence and parseability; numeric validity and warnings still come only from `calculateFreshYeast`
</verification>

<success_criteria>

- All tasks completed
- The homepage presents a real interactive calculator instead of the baseline placeholder
- Users can edit temperature, time, flour quantity, and flour unit and see live updates with no submit control
- Valid states show both recipe yeast grams and normalized grams-per-kilogram values from the domain engine
- Missing, malformed, zero, and negative inputs suspend the result cleanly and present clear field guidance plus invalid feedback in both field-level and result-summary surfaces
- Presets update only temperature and time, and reset returns the exact initial calculator experience
- Integration and e2e coverage prove the live calculator flow and replace the old placeholder-only assertions
- Phase 3 satisfies CALC-01, CALC-02, CALC-03, CALC-04, CALC-05, and SAFE-01 without leaking premium presentation work from later phases
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-calculator-flow/03-01-SUMMARY.md`
</output>