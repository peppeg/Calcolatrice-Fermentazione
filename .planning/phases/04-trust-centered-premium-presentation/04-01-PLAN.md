---
phase: 04-trust-centered-premium-presentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - components/calculator/interactive-calculator.tsx
  - components/calculator/calculator-result-panel.tsx
  - components/calculator/model-transparency-panel.tsx
  - tests/integration/home-page.test.tsx
  - tests/e2e/home.spec.ts
autonomous: true
requirements:
  - TRAN-01
  - TRAN-02
  - TRAN-03
  - UX-01
  - UX-02
  - UX-03
user_setup: []
must_haves:
  truths:
    - The homepage remains a focused standalone calculator experience and does not drift into suite navigation, extra tools, or advanced-control preview UI.
    - The active formula, practical limits, and disclaimer are visible to the user in a dedicated explanatory block, but remain secondary to the input and result workflow.
    - The result surface becomes more trustworthy by summarizing the active scenario, estimator context, and experimental-modifier status without changing the active calculation semantics.
    - Mobile hierarchy remains hero first, then inputs, then result, then model explanation; desktop becomes a stable two-column tool composition with the transparency block directly below.
    - Motion is implemented only as subtle orientation polish and never becomes necessary to understand results, warnings, or state transitions.
    - The phase preserves the existing live calculator contract, warning behavior, accessible labels, and server-page plus client-calculator boundary.
  artifacts:
    - path: app/page.tsx
      provides: premium page shell composition and editorial hero hierarchy
      contains: Home
    - path: components/calculator/interactive-calculator.tsx
      provides: client-side calculator layout orchestration and success-state context shaping
      contains: InteractiveCalculator
    - path: components/calculator/calculator-result-panel.tsx
      provides: trustworthy result presentation with scenario context and estimator status
      contains: CalculatorResultPanel
    - path: components/calculator/model-transparency-panel.tsx
      provides: dedicated explanatory surface for formula, limits, and disclaimer
      contains: ModelTransparencyPanel
    - path: tests/integration/home-page.test.tsx
      provides: component-level regression coverage for premium layout, transparency copy, and stable calculator behavior
      contains: home page calculator flow
    - path: tests/e2e/home.spec.ts
      provides: browser-level proof that the premium composition remains interactive and accessible
      contains: calculator flow is reachable, interactive, and accessible
  key_links:
    - from: app/page.tsx
      to: components/calculator/interactive-calculator.tsx
      via: the server shell composes the live calculator into the premium editorial page without moving interaction ownership out of the client boundary
      pattern: InteractiveCalculator
    - from: app/page.tsx
      to: components/calculator/model-transparency-panel.tsx
      via: the page places the explanatory model block immediately below the tool to keep formula visibility secondary but explicit
      pattern: ModelTransparencyPanel
    - from: components/calculator/interactive-calculator.tsx
      to: components/calculator/calculator-result-panel.tsx
      via: a richer success view model supplies scenario context, modifier status, and warning context to the mounted result region
      pattern: result|warning|summary|state
    - from: components/calculator/calculator-result-panel.tsx
      to: tests/integration/home-page.test.tsx
      via: integration coverage locks the trust-layer wording and active-parameter summary instead of only checking numeric outputs
      pattern: scenario|formula|stima|correttivi
---

<objective>
Plan and execute the full Phase 4 premium presentation pass in one coherent wave.

Purpose: Elevate the homepage from a technically solid calculator into a premium, editorial, and honest product surface by refining hierarchy, layout, transparency, and result context around the existing live estimator.
Output: A premium page shell, a dedicated model-transparency block, a more trustworthy result presentation, and regression coverage that locks the new trust-centered UI contract.
</objective>

<execution_context>
@C:/Users/peppeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/peppeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-trust-centered-premium-presentation/04-CONTEXT.md
@.planning/phases/04-trust-centered-premium-presentation/04-RESEARCH.md
@.planning/phases/04-trust-centered-premium-presentation/04-VALIDATION.md
@.planning/phases/03-interactive-calculator-flow/03-01-PLAN.md
@.planning/phases/03.1-empirical-model-realignment/03.1-01-PLAN.md
@base.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine the homepage shell into the locked premium hierarchy</name>
  <files>app/page.tsx, components/calculator/interactive-calculator.tsx</files>
  <action>Refine the existing homepage composition so it reads as an editorial, sober product rather than a technical demo. Keep `app/page.tsx` server-rendered and preserve the dedicated client calculator boundary, but tighten the hero copy hierarchy, improve page rhythm, and enforce the locked structure: on mobile the user reads hero, then inputs, then result, then model block; on desktop the tool area reads as a stable two-column composition. Any visual refinements should come from spacing, typography, surface treatment, and restrained transitions rather than novelty patterns. Do not add suite navigation, advanced controls, or any feature that belongs to later phases.</action>
  <verify>
    <automated>npm run test:integration</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The page shell looks intentional and premium while preserving the established server/client boundary and the locked layout order.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dedicated model-transparency block and strengthen result trust context</name>
  <files>components/calculator/interactive-calculator.tsx, components/calculator/calculator-result-panel.tsx, components/calculator/model-transparency-panel.tsx, app/page.tsx</files>
  <action>Create a dedicated presentational component for model transparency and mount it immediately below the calculator. That block must make the base formula visible, explain the variables in plain language, state the practical reliability window, and present a sober disclaimer that the estimate is empirical rather than absolute. In parallel, enrich the result panel so success states summarize the active scenario and the current estimator context, including an explicit inactive status for experimental modifiers in v1. Keep the dry-yeast conversion note concise and explicit. This task must satisfy the trust requirements without altering calculation behavior, warning semantics, or introducing any interactive advanced section from Phase 5.</action>
  <verify>
    <automated>npm run test:integration</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The user can understand what model is being used, where it is reliable, how to read the result, and what is currently inactive in the product, all without leaving the main page.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the premium trust surface with browser and integration regression coverage</name>
  <files>tests/integration/home-page.test.tsx, tests/e2e/home.spec.ts</files>
  <action>Extend the current user-facing tests so they prove the premium presentation contract instead of only the numeric calculator flow. Integration coverage must verify the refined hero, the presence of the model-transparency block, visible formula and disclaimer copy, stable success rendering with active-parameter summary and experimental-modifier status, continued warning visibility, and continued dry-yeast context. Keep the Playwright smoke lightweight but meaningful: the page should load, remain interactive, expose the transparency layer, and still pass the accessibility scan after the visual refactor. Preserve stable selectors and labels wherever possible so the test suite guards behavior rather than incidental styling trivia.</action>
  <verify>
    <automated>npm run test:integration</automated>
    <automated>npm run validate:quick</automated>
    <automated>npm run test:e2e</automated>
  </verify>
  <done>The repository has durable protection for the premium trust-centered composition and not only for the raw calculator math.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test:integration succeeds
- [ ] npm run typecheck succeeds
- [ ] npm run validate:quick succeeds
- [ ] npm run test:e2e succeeds
- [ ] `app/page.tsx` remains a server-rendered composition layer that mounts the client calculator rather than absorbing calculator state itself
- [ ] The model formula, limits, and disclaimer are visible in a dedicated secondary block instead of being hidden or collapsed into a tooltip-only pattern
- [ ] The success-state result surface includes active scenario context and the inactive experimental-modifier status without changing the calculation contract
- [ ] Mobile and desktop hierarchy remain aligned with the locked context decisions, and motion stays subtle enough that meaning never depends on animation
</verification>

<success_criteria>

- All tasks completed
- The homepage feels premium, sober, and editorial on both mobile and desktop
- Input, result, and model explanation are visually distinct and easy to scan without confusion
- Users can see the active formula, understand its variables, read a clear disclaimer, and see the practical model limits directly on the page
- The result area provides trustworthy context by summarizing the active scenario and clearly stating that experimental modifiers are inactive in v1
- The calculator remains fast, stable, and behaviorally identical in its live update and warning semantics
- Integration and e2e coverage protect the new trust-centered composition and accessibility after the visual refactor
- Phase 4 satisfies TRAN-01, TRAN-02, TRAN-03, UX-01, UX-02, and UX-03 without prematurely absorbing Phase 5 scope
</success_criteria>

<output>
After completion, create `.planning/phases/04-trust-centered-premium-presentation/04-01-SUMMARY.md`
</output>
