---
phase: 03.1-empirical-model-realignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/calc/constants.ts
  - lib/calc/model.ts
  - lib/calc/validation.ts
  - lib/calc/calculate-fresh-yeast.ts
  - lib/calc/index.ts
  - tests/unit/calc/fixtures/model-calibration-fixtures.ts
  - tests/unit/calc/model.test.ts
  - tests/unit/calc/calculate-fresh-yeast.test.ts
  - tests/unit/calc/validation.test.ts
  - tests/integration/home-page.test.tsx
autonomous: true
requirements:
  - MODEL-01
  - MODEL-02
user_setup: []
must_haves:
  truths:
    - The active v1 calculator still exposes one ambient-use estimator and does not introduce any user-facing model selector or runtime lookup-table truth.
    - Model realignment starts from a repo-owned calibration fixture corpus with explicit acceptance tolerances, so execution can prove whether coefficient retuning is sufficient before escalating formula shape.
    - The runtime contract centered on `calculateFreshYeast` stays stable: normalization, invalid short-circuiting, warning semantics, rounding behavior, and result shape remain intact unless calibration evidence explicitly requires a warning-range update.
    - If coefficient retuning cannot satisfy the fixture tolerances, the fallback is a simple piecewise time family with enforced continuity and monotonic behavior, not arbitrary cliffs or hidden complexity.
    - Regression coverage becomes independent of the old formula constants by locking fixture fit, mathematical invariants, and calculator-flow compatibility instead of re-deriving truth from the implementation under test.
  artifacts:
    - path: tests/unit/calc/fixtures/model-calibration-fixtures.ts
      provides: curated calibration corpus and acceptance metadata for the realigned model
      contains: expectedGramsPerKg
    - path: lib/calc/model.ts
      provides: active internal model seam and the chosen realigned formula family
      contains: calculateBaseYeastPerKg
    - path: lib/calc/calculate-fresh-yeast.ts
      provides: stable public calculation orchestrator preserved across model realignment
      contains: calculateFreshYeast
    - path: tests/unit/calc/model.test.ts
      provides: independent fit, monotonicity, and continuity verification for the active model
      contains: calibration fixtures
    - path: tests/unit/calc/calculate-fresh-yeast.test.ts
      provides: calculator-contract regression protection independent from raw model internals
      contains: calculateFreshYeast
    - path: tests/integration/home-page.test.tsx
      provides: proof that the live calculator UI still tolerates the realigned model without stale-output regressions
      contains: warning|result
  key_links:
    - from: tests/unit/calc/fixtures/model-calibration-fixtures.ts
      to: tests/unit/calc/model.test.ts
      via: fixture-led acceptance criteria become the test truth for model fitness
      pattern: expectedGramsPerKg|tolerance|weight
    - from: lib/calc/model.ts
      to: lib/calc/calculate-fresh-yeast.ts
      via: public calculator orchestration consumes the active internal model without changing its external contract
      pattern: calculateBaseYeastPerKg
    - from: lib/calc/constants.ts
      to: lib/calc/validation.ts
      via: empirical reliability ranges remain explicit and only change together with warning logic if calibration evidence justifies it
      pattern: EMPIRICAL_(TEMPERATURE|TIME)_RANGE
    - from: tests/unit/calc/model.test.ts
      to: tests/unit/calc/calculate-fresh-yeast.test.ts
      via: independent numeric truth and orchestration regression checks cover both model credibility and product contract
      pattern: fixture|warning|status
---

<objective>
Execute the full Phase 03.1 corrective realignment in one coherent pass.

Purpose: Re-anchor the active v1 model to defendable calibration fixtures, attempt the cheapest viable retune first, and preserve the already-complete calculator reliability and UI behavior while improving numeric trust.
Output: A fixture-led model definition inside `lib/calc`, an explicit retune-versus-piecewise decision gate, and regression coverage that proves the calculator contract survived the recalibration.
</objective>

<execution_context>
@C:/Users/peppeg/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/peppeg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.1-empirical-model-realignment/03.1-CONTEXT.md
@.planning/phases/03.1-empirical-model-realignment/03.1-RESEARCH.md
@.planning/phases/03.1-empirical-model-realignment/03.1-VALIDATION.md
@.planning/phases/02-domain-calculation-engine/02-01-PLAN.md
@.planning/phases/03-interactive-calculator-flow/03-01-PLAN.md
@base.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the calibration fixture corpus and acceptance gate</name>
  <files>tests/unit/calc/fixtures/model-calibration-fixtures.ts, tests/unit/calc/model.test.ts</files>
  <action>Create a repo-owned calibration fixture module for standard ambient-use scenarios that the product is willing to defend. Each fixture must capture `temperatureC`, `timeHours`, expected `gramsPerKg`, source label, confidence weight, and any note needed to distinguish core-range targets from comparison-only evidence. Encode explicit acceptance tolerances in the test layer so execution can measure whether the current single-family formula can be retuned successfully without subjective debate. Update `tests/unit/calc/model.test.ts` to consume the fixtures as independent truth and to report worst-fit failures clearly, while also preserving basic monotonic expectations for time and temperature.</action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The phase has a concrete calibration target and a measurable pass/fail gate for deciding whether coefficient retuning is enough.</done>
</task>

<task type="auto">
  <name>Task 2: Realign the active model behind an internal seam with retune-first and piecewise fallback rules</name>
  <files>lib/calc/constants.ts, lib/calc/model.ts, lib/calc/validation.ts, lib/calc/calculate-fresh-yeast.ts, lib/calc/index.ts</files>
  <action>Refactor the model internals so `calculateBaseYeastPerKg` remains the stable exported entrypoint while the active empirical definition can change behind it. Attempt coefficient retuning inside the current separable family first, judged only against the fixture acceptance gate established in Task 1. If that retune satisfies the agreed tolerances, keep the runtime model as one compact closed form and remove any formula-specific exports that would become misleading. If coefficient-only retuning fails to satisfy short-time trust without harming the rest of the core range, escalate once to a simple piecewise time-based family that remains smooth, monotonic, and explainable, with enforced continuity at the transition. Preserve `calculateFreshYeast` behavior, rounding, modifier no-op semantics, and UI-facing result shape; only adjust empirical warning ranges and related constants if calibration evidence clearly justifies it, and update validation ownership in the same pass if that happens.</action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The active v1 model is numerically realigned behind a stable public calculator contract, with the lowest-complexity viable formula family chosen by evidence instead of taste.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh regression coverage for model invariants, warnings, and live calculator compatibility</name>
  <files>tests/unit/calc/model.test.ts, tests/unit/calc/calculate-fresh-yeast.test.ts, tests/unit/calc/validation.test.ts, tests/integration/home-page.test.tsx</files>
  <action>Replace literal assertions against the old hard-coded formula with regression coverage that proves the new model remains sane and the product surface remains stable. Extend `model.test.ts` with fixture-fit assertions, monotonic sweeps, and, if Task 2 escalated to a piecewise family, continuity checks immediately below, at, and above the transition window. Update `calculate-fresh-yeast.test.ts` so at least one happy-path expectation is anchored to independent fixture numbers instead of values recomputed from the same helper under test, while still verifying invalid short-circuiting, warning behavior, and modifier no-op output. Touch `validation.test.ts` and `tests/integration/home-page.test.tsx` only as needed to reflect an evidence-backed warning-range or warning-copy adjustment; otherwise preserve the existing Phase 3 UI contract unchanged and prove the live result surface still behaves correctly for valid, invalid, and warned states.</action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npm run test:integration</automated>
    <automated>npm run build</automated>
  </verify>
  <done>The codebase can prove both numeric credibility and calculator-contract safety without relying on tautological formula assertions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test:unit succeeds
- [ ] npm run test:integration succeeds
- [ ] npm run typecheck succeeds
- [ ] npm run build succeeds
- [ ] The active runtime model is still one declared ambient-use estimator rather than a table-driven runtime truth
- [ ] `calculateFreshYeast` keeps its existing discriminated result contract and valid/invalid warning semantics
- [ ] The chosen formula family satisfies the fixture tolerances, and any piecewise transition has explicit continuity coverage
- [ ] Warning-range changes, if any, are evidence-backed and accompanied by matching validation and UI regression updates
</verification>

<success_criteria>

- All tasks completed
- The repo contains a defendable calibration fixture corpus for Phase 03.1
- Execution makes a clear evidence-based choice between coefficient retuning and a simple piecewise time family
- The active model truth improves short-time credibility without introducing numerical cliffs or user-facing multi-model complexity
- `calculateFreshYeast` and the live calculator flow remain structurally unchanged from the user’s perspective
- Regression coverage proves fixture fit, monotonic behavior, calculator-contract preservation, and warning compatibility
- Phase 03.1 stays inside corrective model-truth scope and does not absorb Phase 4 presentation work
</success_criteria>
<output>
After completion, create `.planning/phases/03.1-empirical-model-realignment/03.1-01-SUMMARY.md`
</output>



